import { Box } from "@mui/material";
import { styled } from "@mui/system";

const WrapComponent = styled(Box)({
    padding : '1.5rem 1.5rem 0.75rem 1.5rem',
    width : '100%',
    marginBottom : '1rem',
    borderRadius : '1rem'
});

export default WrapComponent;